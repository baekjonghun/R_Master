#R을 실행시 패키지를 보여준다.
getOption('defaultPackages')

#사용될 수 있는 패키지 목록과 현재의 환경겍체를 보여준다.
search()

#설치 되어 있는 모든 패키지 목록 및 의존정보 등을 보여준다. 
installed.packages()


#기본 라이브러리 폴더에 설치되어 있는 모든 패키지들 및 함수들을 별도의 창을 띄어 보여준다.
library()
